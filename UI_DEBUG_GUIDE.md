# UI 样式调试指南

## 🎨 方法概览

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **Orbit 实时调试** | 最真实的环境 | 需要频繁刷新 | 最终测试 |
| **预览页面** | 快速迭代 | 环境不完全真实 | 快速调试样式 |
| **浏览器 DevTools** | 实时修改 | 需手动应用到代码 | 微调样式 |
| **Storybook/组件隔离** | 专注单个组件 | 需额外配置 | 组件开发 |

---

## 方法 1：Orbit 页面实时调试 ⭐ **（推荐）**

这是最准确的方式，可以在真实环境中看到效果。

### 步骤：

#### 1. 构建并安装脚本

```bash
# 构建脚本
npm run build

# 生成的文件位于：dist/orbit-exporter.user.js
```

#### 2. 安装到 Tampermonkey

- 打开 Tampermonkey 管理面板
- 点击"添加新脚本"
- 复制 `dist/orbit-exporter.user.js` 内容并粘贴
- 保存（Ctrl+S 或 Cmd+S）

#### 3. 访问 Twitter

打开 https://twitter.com 或 https://x.com

#### 4. 打开浏览器开发者工具

按 `F12` 或：
- **Chrome/Edge**: `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
- **Firefox**: `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
- **Safari**: `Cmd+Option+I` (需先在设置中启用开发者菜单)

#### 5. 调试 UI 样式

**Elements 面板：**
1. 点击左上角的"选择元素"图标（或按 `Ctrl+Shift+C`）
2. 悬停在你想调试的 UI 元素上
3. 点击选中该元素
4. 在右侧 Styles 面板中可以：
   - ✏️ 修改现有样式
   - ➕ 添加新的 CSS 规则
   - 🎨 使用颜色选择器调整颜色
   - 📏 调整边距、内边距等

**实时修改示例：**

```css
/* 在 Elements 面板中，找到你的元素，然后在 Styles 中添加/修改 */

/* 修改控制面板宽度 */
#twe-root > section {
  width: 400px !important;
}

/* 修改按钮样式 */
.btn {
  background: #1d9bf0 !important;
  border-radius: 20px !important;
}

/* 修改字体大小 */
#twe-root {
  font-size: 14px !important;
}
```

#### 6. 应用修改到源代码

找到满意的样式后：

1. 复制修改后的 CSS
2. 在源代码中找到对应的文件（通常在 `src/` 目录）
3. 应用修改
4. 重新构建：`npm run build`
5. 刷新 Twitter 页面查看效果

### 快速迭代技巧：

创建一个监听文件变化并自动构建的脚本：

```bash
# 安装 nodemon（如果还没安装）
npm install -g nodemon

# 监听文件变化并自动构建
nodemon --watch src --exec "npm run build"
```

然后只需：
1. 修改代码
2. 等待自动构建完成
3. 刷新 Twitter 页面

---

## 方法 2：预览页面调试

使用我创建的 `preview.html` 页面。

### 步骤：

#### 1. 启动开发服务器

```bash
npm run dev
```

浏览器会自动打开 http://localhost:5173/preview.html

#### 2. 加载脚本

开发服务器会自动注入脚本，你应该能看到 UI 组件。

如果没有，在浏览器控制台运行：

```javascript
// 方式 1: 加载构建后的脚本
const script = document.createElement('script');
script.src = '/dist/orbit-exporter.user.js';
document.head.appendChild(script);

// 方式 2: 直接导入模块 (开发模式)
import('/src/main.tsx');
```

#### 3. 调试样式

使用浏览器 DevTools（同方法 1）调试样式。

#### 4. 热重载

如果设置正确，修改代码后页面会自动刷新。

### 注意事项：

- ⚠️ 预览页面环境与 Twitter 不完全相同
- ⚠️ 某些依赖 Twitter API 的功能可能无法工作
- ✅ 适合快速调试布局和样式

---

## 方法 3：浏览器 DevTools 进阶技巧

### 技巧 1：复制元素样式

1. 右键点击元素 → Inspect
2. 在 Styles 面板中右键 → Copy → Copy all declarations
3. 粘贴到你的 CSS/TSX 文件中

### 技巧 2：查看计算后的样式

在 DevTools 中切换到 "Computed" 标签页：
- 查看最终应用的样式
- 查看样式来源
- 查看盒模型（Box Model）

### 技巧 3：模拟不同状态

在 Elements 面板中：
1. 选中元素
2. 点击 `:hov` 按钮
3. 勾选 `:hover`、`:focus`、`:active` 等状态
4. 调试这些状态下的样式

### 技巧 4：截图对比

```javascript
// 在控制台运行，截图当前 UI
const element = document.querySelector('#twe-root');
const canvas = document.createElement('canvas');
const context = canvas.getContext('2d');
// ... 实现截图逻辑
```

或使用浏览器扩展如：
- Full Page Screen Capture
- Awesome Screenshot

### 技巧 5：CSS 调试技巧

```css
/* 添加边框查看布局 */
* {
  outline: 1px solid red !important;
}

/* 查看某个容器 */
#twe-root > section {
  outline: 2px solid blue !important;
}
```

---

## 方法 4：组件样式文件定位

### 快速找到样式所在文件：

```bash
# 搜索特定的 class 名
grep -r "card-compact" src/

# 搜索特定的样式属性
grep -r "bg-base-100" src/

# 搜索特定组件
find src/ -name "*settings*"
```

### 主要样式文件：

```
src/
├── core/
│   ├── app.tsx          # 主应用布局
│   └── settings.tsx     # 设置面板（你刚才修改的）
├── components/
│   ├── common.tsx       # 通用组件
│   ├── module-ui.tsx    # 模块 UI
│   └── table/           # 表格组件
├── index.css            # 全局样式
└── utils/
    └── auto-scroller.ts # 不包含样式，但可以调试逻辑
```

### DaisyUI 样式系统：

这个项目使用 [DaisyUI](https://daisyui.com/)，它基于 Tailwind CSS。

常用的 class：
```tsx
// 卡片
<div class="card card-compact bg-base-100">

// 按钮
<button class="btn btn-primary">

// 输入框
<input class="input input-bordered">

// 切换开关
<input type="checkbox" class="toggle toggle-primary">
```

查看 DaisyUI 文档：https://daisyui.com/components/

---

## 方法 5：样式覆盖技巧

### 方式 1：直接修改组件样式

在 TSX 文件中：

```tsx
// src/core/settings.tsx
<div class="my-custom-class bg-base-100 p-4">
  {/* content */}
</div>
```

### 方式 2：使用 style 属性

```tsx
<div style={{ width: '400px', background: '#1d9bf0' }}>
  {/* content */}
</div>
```

### 方式 3：添加全局样式

在 `src/index.css` 中：

```css
#twe-root {
  /* 你的自定义样式 */
}

#twe-root .my-custom-class {
  /* 更具体的样式 */
}
```

---

## 常见调试场景

### 场景 1：修改控制面板宽度

**文件**: `src/core/app.tsx`

找到：
```tsx
class="card card-compact bg-base-100 fixed border shadow-xl w-80 ..."
```

修改 `w-80` 为其他宽度：
- `w-96` (384px)
- `w-[400px]` (自定义 400px)

### 场景 2：修改设置面板布局

**文件**: `src/core/settings.tsx`

找到：
```tsx
<Modal title={t('Settings')} show={showSettings} onClose={toggleSettings} class="max-w-lg">
```

修改 `max-w-lg` 为：
- `max-w-xl` (更宽)
- `max-w-2xl` (更更宽)

### 场景 3：修改按钮样式

**文件**: `src/core/settings.tsx` 或其他组件文件

```tsx
<button class="btn btn-xs btn-primary">
  {t('Start')}
</button>
```

可用的 DaisyUI 按钮类：
- `btn-primary` (主要按钮)
- `btn-secondary` (次要按钮)
- `btn-success` (成功)
- `btn-error` (错误)
- `btn-warning` (警告)

### 场景 4：调整间距

Tailwind 间距 class：
```tsx
// 内边距
p-4   // padding: 16px (4 * 4px)
px-2  // padding-left & padding-right: 8px
py-3  // padding-top & padding-bottom: 12px

// 外边距
m-4   // margin: 16px
mx-2  // margin-left & margin-right: 8px
my-3  // margin-top & margin-bottom: 12px

// 间隙
gap-2 // gap: 8px (用于 flex 或 grid)
```

---

## 推荐工作流程

### 日常开发：

1. **启动监听构建**
   ```bash
   nodemon --watch src --exec "npm run build"
   ```

2. **打开 Twitter** 并启用脚本

3. **打开 DevTools** (F12)

4. **修改代码** → 等待自动构建 → 刷新页面

### 快速样式调试：

1. **在 Twitter 上打开 DevTools**

2. **直接在 DevTools 中修改样式**，实时查看效果

3. **满意后复制修改** → 应用到源代码

4. **重新构建** → 验证

---

## 故障排除

### 问题 1：样式没有生效

可能原因：
- CSS 优先级不够，添加 `!important`
- 样式被 DaisyUI 覆盖，使用更具体的选择器
- 没有重新构建，运行 `npm run build`

### 问题 2：页面布局错乱

检查：
- 是否修改了关键的布局 class
- 是否影响了 Tailwind/DaisyUI 的 class
- 在 DevTools 中禁用你的修改，看是否恢复

### 问题 3：样式在不同浏览器表现不同

- 使用 DevTools 的设备模拟功能测试
- 检查 CSS 前缀是否正确（Autoprefixer 应该自动处理）
- 使用标准的 CSS 属性

### 问题 4：开发服务器无法启动

```bash
# 清除缓存
rm -rf node_modules
npm install

# 重新启动
npm run dev
```

---

## 有用的工具和扩展

### Chrome/Edge 扩展：

1. **React Developer Tools** - 查看组件树（虽然这是 Preact，但大部分功能可用）
2. **CSS Peeper** - 快速查看页面样式
3. **WhatFont** - 识别字体
4. **ColorZilla** - 取色器
5. **Pesticide** - 显示元素边框，方便查看布局

### VS Code 扩展：

1. **Tailwind CSS IntelliSense** - Tailwind class 自动补全
2. **CSS Peek** - 快速跳转到 CSS 定义
3. **Prettier** - 代码格式化
4. **ESLint** - 代码检查

---

## 快速参考

### 常用快捷键：

| 功能 | Windows/Linux | Mac |
|------|---------------|-----|
| 打开 DevTools | `F12` | `Cmd+Option+I` |
| 选择元素 | `Ctrl+Shift+C` | `Cmd+Shift+C` |
| 刷新页面 | `Ctrl+R` | `Cmd+R` |
| 强制刷新 | `Ctrl+Shift+R` | `Cmd+Shift+R` |
| 保存文件 | `Ctrl+S` | `Cmd+S` |
| 搜索文件 | `Ctrl+P` | `Cmd+P` |

### 常用 Tailwind class：

```
宽度: w-full, w-80, w-96, w-[400px]
高度: h-full, h-screen, h-auto
背景: bg-base-100, bg-primary, bg-white
文字: text-sm, text-base, text-lg, text-white
圆角: rounded, rounded-md, rounded-lg, rounded-full
阴影: shadow, shadow-md, shadow-lg, shadow-xl
边距: m-4, mx-2, my-3, mt-1, mr-2, mb-3, ml-4
内距: p-4, px-2, py-3, pt-1, pr-2, pb-3, pl-4
```

---

## 总结

**推荐调试流程：**

1. 🚀 **启动**: `npm run dev` 或配置自动构建
2. 🌐 **打开**: Twitter 页面 + DevTools
3. 🎨 **调试**: 在 DevTools 中实时修改样式
4. ✏️ **应用**: 将满意的样式复制到源代码
5. 🔄 **构建**: `npm run build` 并刷新页面
6. ✅ **验证**: 确认修改生效

**最佳实践：**

- ✅ 在真实 Twitter 环境中测试
- ✅ 使用 DevTools 快速迭代
- ✅ 遵循 DaisyUI/Tailwind 的设计系统
- ✅ 测试不同屏幕尺寸
- ✅ 提交前清理调试代码

祝调试顺利！🎉
